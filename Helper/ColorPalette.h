#ifndef COLORPALETTE
#define COLORPALETTE

#include "SFML/Graphics.hpp"

struct ColorPalette
{
	inline static const sf::Color lightGray = sf::Color( 200, 200, 190 );
	inline static const sf::Color charcoal = sf::Color(28, 28, 28);
	inline static const sf::Color darkGray = sf::Color(80, 80, 80);
	inline static const sf::Color orange = sf::Color(255, 149, 0);
};

#endif